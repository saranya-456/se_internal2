# se_internal2
create jenkins scripted pipeline for online recruitment system using a given github repo, expected o/p is a working scripted pipeline that automates and builds test stages successfully
configure git webhoks to trigger jenkins automatically:
create a webhook in the github repository to notify jenkins on every code push.
disable poll scm and verify jenkins start builds instantly when commits are pushed
demonstrate the working of webhook trigger thru build history
expected o/p:
jenkins pipeline triggered automatically via github webhook without manual or scm
